apiVersion: apps/v1
kind: Deployment
metadata:
  name: issue-service
  namespace: civicconnect
  labels:
    app: issue-service
    version: v1.0.0
    component: microservice
spec:
  replicas: 2
  selector:
    matchLabels:
      app: issue-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: issue-service
        version: v1.0.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3002"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
      containers:
        - name: issue-service
          image: ghcr.io/YOUR_GITHUB_USERNAME/civicconnect-issue-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3002
              name: http
          env:
            - name: PORT
              value: "3002"
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: civicconnect-config
                  key: NODE_ENV
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: mongo-uris
                  key: ISSUE_MONGO_URI
            - name: NVIDIA_API_KEY
              valueFrom:
                secretKeyRef:
                  name: civicconnect-secrets
                  key: NVIDIA_API_KEY
                  optional: true
            - name: ASSIGNMENT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: civicconnect-config
                  key: ASSIGNMENT_SERVICE_URL
            - name: NOTIFICATION_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: civicconnect-config
                  key: NOTIFICATION_SERVICE_URL
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
      terminationGracePeriodSeconds: 30
